-- Program to show use of SQL%FOUND (implicit cursor)

BEGIN

DELETE FROM STUDENT WHERE GRADE='C';
IF(SQL%FOUND) THEN
DBMS_OUTPUT.PUT_LINE(SQL%ROWCOUNT||'ROWS DELETED');
ELSE
DBMS_OUTPUT.PUT_LINE('NO ROWS DELETED');
END IF;

END;
